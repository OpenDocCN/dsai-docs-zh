- en: numpy.trace
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: numpy.trace
- en: 原文：[https://numpy.org/doc/1.26/reference/generated/numpy.trace.html](https://numpy.org/doc/1.26/reference/generated/numpy.trace.html)
  id: totrans-1
  prefs:
  - PREF_BQ
  type: TYPE_NORMAL
  zh: 原文：[https://numpy.org/doc/1.26/reference/generated/numpy.trace.html](https://numpy.org/doc/1.26/reference/generated/numpy.trace.html)
- en: '[PRE0]'
  id: totrans-2
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
- en: Return the sum along diagonals of the array.
  id: totrans-3
  prefs: []
  type: TYPE_NORMAL
  zh: 返回数组沿对角线的总和。
- en: If *a* is 2-D, the sum along its diagonal with the given offset is returned,
    i.e., the sum of elements `a[i,i+offset]` for all i.
  id: totrans-4
  prefs: []
  type: TYPE_NORMAL
  zh: 如果 *a* 是 2-D，则返回沿其对角线的总和，并给定偏移量，即，所有 i 的元素 `a[i,i+offset]` 的和。
- en: If *a* has more than two dimensions, then the axes specified by axis1 and axis2
    are used to determine the 2-D sub-arrays whose traces are returned. The shape
    of the resulting array is the same as that of *a* with *axis1* and *axis2* removed.
  id: totrans-5
  prefs: []
  type: TYPE_NORMAL
  zh: 如果 *a* 大于两个维度，则由 axis1 和 axis2 指定的轴用于确定返回的迹为 2-D 子数组。返回数组的形状与 *a* 相同，但删除了 *axis1*
    和 *axis2*。
- en: 'Parameters:'
  id: totrans-6
  prefs: []
  type: TYPE_NORMAL
  zh: '参数:'
- en: '**a**array_like'
  id: totrans-7
  prefs: []
  type: TYPE_NORMAL
  zh: '**a** 类似数组'
- en: Input array, from which the diagonals are taken.
  id: totrans-8
  prefs: []
  type: TYPE_NORMAL
  zh: 被取对角线的输入数组。
- en: '**offset**int, optional'
  id: totrans-9
  prefs: []
  type: TYPE_NORMAL
  zh: '**offset** 整数，可选'
- en: Offset of the diagonal from the main diagonal. Can be both positive and negative.
    Defaults to 0.
  id: totrans-10
  prefs: []
  type: TYPE_NORMAL
  zh: 与主对角线的偏移量。可以为正数或负数。默认为 0。
- en: '**axis1, axis2**int, optional'
  id: totrans-11
  prefs: []
  type: TYPE_NORMAL
  zh: '**axis1, axis2** 整数，可选'
- en: Axes to be used as the first and second axis of the 2-D sub-arrays from which
    the diagonals should be taken. Defaults are the first two axes of *a*.
  id: totrans-12
  prefs: []
  type: TYPE_NORMAL
  zh: 用作应该获取对角线的 2-D 子数组的第一个和第二个轴的轴。默认为 *a* 的前两轴。
- en: '**dtype**dtype, optional'
  id: totrans-13
  prefs: []
  type: TYPE_NORMAL
  zh: '**dtype** 数据类型，可选'
- en: Determines the data-type of the returned array and of the accumulator where
    the elements are summed. If dtype has the value None and *a* is of integer type
    of precision less than the default integer precision, then the default integer
    precision is used. Otherwise, the precision is the same as that of *a*.
  id: totrans-14
  prefs: []
  type: TYPE_NORMAL
  zh: 确定返回数组的数据类型以及元素求和的累加器的数据类型。如果 dtype 的值为 None，并且 *a* 是小于默认整数精度的整数类型，则使用默认整数精度。否则，精度与
    *a* 的精度相同。
- en: '**out**ndarray, optional'
  id: totrans-15
  prefs: []
  type: TYPE_NORMAL
  zh: '**out** 数组，可选'
- en: Array into which the output is placed. Its type is preserved and it must be
    of the right shape to hold the output.
  id: totrans-16
  prefs: []
  type: TYPE_NORMAL
  zh: 数组，其输出需放置的位置。其类型保持不变，必须具有正确的形状以容纳输出。
- en: 'Returns:'
  id: totrans-17
  prefs: []
  type: TYPE_NORMAL
  zh: '返回:'
- en: '**sum_along_diagonals**ndarray'
  id: totrans-18
  prefs: []
  type: TYPE_NORMAL
  zh: '**sum_along_diagonals** 数组'
- en: If *a* is 2-D, the sum along the diagonal is returned. If *a* has larger dimensions,
    then an array of sums along diagonals is returned.
  id: totrans-19
  prefs: []
  type: TYPE_NORMAL
  zh: 如果 *a* 是 2-D，则返回沿对角线的总和。如果 *a* 的维度更大，则返回沿对角线的总和数组。
- en: See also
  id: totrans-20
  prefs: []
  type: TYPE_NORMAL
  zh: 另请参见
- en: '[`diag`](numpy.diag.html#numpy.diag "numpy.diag"), [`diagonal`](numpy.diagonal.html#numpy.diagonal
    "numpy.diagonal"), [`diagflat`](numpy.diagflat.html#numpy.diagflat "numpy.diagflat")'
  id: totrans-21
  prefs: []
  type: TYPE_NORMAL
  zh: '[`diag`](numpy.diag.html#numpy.diag "numpy.diag"), [`diagonal`](numpy.diagonal.html#numpy.diagonal
    "numpy.diagonal"), [`diagflat`](numpy.diagflat.html#numpy.diagflat "numpy.diagflat")'
- en: Examples
  id: totrans-22
  prefs: []
  type: TYPE_NORMAL
  zh: 示例
- en: '[PRE1]'
  id: totrans-23
  prefs: []
  type: TYPE_PRE
  zh: '[PRE1]'
- en: '[PRE2]'
  id: totrans-24
  prefs: []
  type: TYPE_PRE
  zh: '[PRE2]'
