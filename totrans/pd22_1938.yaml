- en: What’s new in 0.25.1 (August 21, 2019)
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 0.25.1 新功能（2019 年 8 月 21 日）
- en: 原文：[https://pandas.pydata.org/docs/whatsnew/v0.25.1.html](https://pandas.pydata.org/docs/whatsnew/v0.25.1.html)
  id: totrans-1
  prefs:
  - PREF_BQ
  type: TYPE_NORMAL
  zh: 原文：[https://pandas.pydata.org/docs/whatsnew/v0.25.1.html](https://pandas.pydata.org/docs/whatsnew/v0.25.1.html)
- en: These are the changes in pandas 0.25.1\. See [Release notes](index.html#release)
    for a full changelog including other versions of pandas.
  id: totrans-2
  prefs: []
  type: TYPE_NORMAL
  zh: 这些是 pandas 0.25.1 的更改。请参阅[发布说明](index.html#release)获取包括其他版本 pandas 的完整更改日志。
- en: IO and LZMA
  id: totrans-3
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: IO 和 LZMA
- en: Some users may unknowingly have an incomplete Python installation lacking the
    `lzma` module from the standard library. In this case, `import pandas` failed
    due to an `ImportError` ([GH 27575](https://github.com/pandas-dev/pandas/issues/27575)).
    pandas will now warn, rather than raising an `ImportError` if the `lzma` module
    is not present. Any subsequent attempt to use `lzma` methods will raise a `RuntimeError`.
    A possible fix for the lack of the `lzma` module is to ensure you have the necessary
    libraries and then re-install Python. For example, on MacOS installing Python
    with `pyenv` may lead to an incomplete Python installation due to unmet system
    dependencies at compilation time (like `xz`). Compilation will succeed, but Python
    might fail at run time. The issue can be solved by installing the necessary dependencies
    and then re-installing Python.
  id: totrans-4
  prefs: []
  type: TYPE_NORMAL
  zh: 一些用户可能无意中安装了缺少标准库中 `lzma` 模块的不完整的 Python。在这种情况下，`import pandas` 会因为 `ImportError`
    而失败 ([GH 27575](https://github.com/pandas-dev/pandas/issues/27575))。pandas 现在会发出警告，而不是在缺少
    `lzma` 模块时引发 `ImportError`。任何后续尝试使用 `lzma` 方法都会引发 `RuntimeError`。解决缺少 `lzma` 模块的可能方法是确保安装了必要的库，然后重新安装
    Python。例如，在 MacOS 上使用 `pyenv` 安装 Python 可能会由于编译时未满足的系统依赖项（如 `xz`）而导致 Python 安装不完整。编译会成功，但
    Python 可能在运行时失败。该问题可以通过安装必要的依赖项然后重新安装 Python 来解决。
- en: '## Bug fixes'
  id: totrans-5
  prefs: []
  type: TYPE_NORMAL
  zh: '## Bug 修复'
- en: Categorical
  id: totrans-6
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 分类
- en: Bug in `Categorical.fillna()` that would replace all values, not just those
    that are `NaN` ([GH 26215](https://github.com/pandas-dev/pandas/issues/26215))
  id: totrans-7
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`Categorical.fillna()` 中的错误，会替换所有值，而不仅仅是 `NaN` 的值 ([GH 26215](https://github.com/pandas-dev/pandas/issues/26215))'
- en: Datetimelike
  id: totrans-8
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 日期时间样式
- en: Bug in `to_datetime()` where passing a timezone-naive `DatetimeArray` or `DatetimeIndex`
    and `utc=True` would incorrectly return a timezone-naive result ([GH 27733](https://github.com/pandas-dev/pandas/issues/27733))
  id: totrans-9
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`to_datetime()` 中的错误，在传递时区未知的 `DatetimeArray` 或 `DatetimeIndex` 且 `utc=True`
    时，将错误地返回一个时区未知的结果 ([GH 27733](https://github.com/pandas-dev/pandas/issues/27733))'
- en: Bug in `Period.to_timestamp()` where a `Period` outside the `Timestamp` implementation
    bounds (roughly 1677-09-21 to 2262-04-11) would return an incorrect `Timestamp`
    instead of raising `OutOfBoundsDatetime` ([GH 19643](https://github.com/pandas-dev/pandas/issues/19643))
  id: totrans-10
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`Period.to_timestamp()` 中的错误，其中 `Period` 超出 `Timestamp` 实现范围（大约 1677-09-21
    至 2262-04-11）会返回一个不正确的 `Timestamp` 而不是引发 `OutOfBoundsDatetime` ([GH 19643](https://github.com/pandas-dev/pandas/issues/19643))'
- en: Bug in iterating over `DatetimeIndex` when the underlying data is read-only
    ([GH 28055](https://github.com/pandas-dev/pandas/issues/28055))
  id: totrans-11
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在迭代 `DatetimeIndex` 时，当底层数据是只读时会出错 ([GH 28055](https://github.com/pandas-dev/pandas/issues/28055))
- en: Timezones
  id: totrans-12
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 时区
- en: Bug in `Index` where a numpy object array with a timezone aware `Timestamp`
    and `np.nan` would not return a `DatetimeIndex` ([GH 27011](https://github.com/pandas-dev/pandas/issues/27011))
  id: totrans-13
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`Index` 中的错误，其中一个带有时区感知 `Timestamp` 和 `np.nan` 的 numpy 对象数组不会返回 `DatetimeIndex`
    ([GH 27011](https://github.com/pandas-dev/pandas/issues/27011))'
- en: Numeric
  id: totrans-14
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 数值
- en: Bug in `Series.interpolate()` when using a timezone aware `DatetimeIndex` ([GH
    27548](https://github.com/pandas-dev/pandas/issues/27548))
  id: totrans-15
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当使用时区感知的 `DatetimeIndex` 时，`Series.interpolate()` 中的错误 ([GH 27548](https://github.com/pandas-dev/pandas/issues/27548))
- en: Bug when printing negative floating point complex numbers would raise an `IndexError`
    ([GH 27484](https://github.com/pandas-dev/pandas/issues/27484))
  id: totrans-16
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 打印负浮点复数时会引发 `IndexError` 的错误 ([GH 27484](https://github.com/pandas-dev/pandas/issues/27484))
- en: Bug where `DataFrame` arithmetic operators such as `DataFrame.mul()` with a
    `Series` with axis=1 would raise an `AttributeError` on `DataFrame` larger than
    the minimum threshold to invoke numexpr ([GH 27636](https://github.com/pandas-dev/pandas/issues/27636))
  id: totrans-17
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当 `DataFrame` 算术运算符（如 `DataFrame.mul()`）与 `axis=1` 的 `Series` 一起在大于调用 numexpr
    的最小阈值的 `DataFrame` 上时会引发 `AttributeError` ([GH 27636](https://github.com/pandas-dev/pandas/issues/27636))
- en: Bug in `DataFrame` arithmetic where missing values in results were incorrectly
    masked with `NaN` instead of `Inf` ([GH 27464](https://github.com/pandas-dev/pandas/issues/27464))
  id: totrans-18
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`DataFrame` 算术中的错误，结果中缺失值被错误地掩盖为 `NaN` 而不是 `Inf` ([GH 27464](https://github.com/pandas-dev/pandas/issues/27464))'
- en: Conversion
  id: totrans-19
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 转换
- en: Improved the warnings for the deprecated methods `Series.real()` and `Series.imag()`
    ([GH 27610](https://github.com/pandas-dev/pandas/issues/27610))
  id: totrans-20
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 改进了已弃用方法`Series.real()`和`Series.imag()`的警告信息 ([GH 27610](https://github.com/pandas-dev/pandas/issues/27610))
- en: Interval
  id: totrans-21
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 区间
- en: Bug in `IntervalIndex` where `dir(obj)` would raise `ValueError` ([GH 27571](https://github.com/pandas-dev/pandas/issues/27571))
  id: totrans-22
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了`IntervalIndex`中的错误，其中`dir(obj)`会引发`ValueError` ([GH 27571](https://github.com/pandas-dev/pandas/issues/27571))
- en: Indexing
  id: totrans-23
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 索引
- en: Bug in partial-string indexing returning a NumPy array rather than a `Series`
    when indexing with a scalar like `.loc['2015']` ([GH 27516](https://github.com/pandas-dev/pandas/issues/27516))
  id: totrans-24
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了部分字符串索引中的错误，当使用类似`.loc['2015']`这样的标量进行索引时，返回的不是`Series`而是一个NumPy数组 ([GH 27516](https://github.com/pandas-dev/pandas/issues/27516))
- en: Break reference cycle involving `Index` and other index classes to allow garbage
    collection of index objects without running the GC. ([GH 27585](https://github.com/pandas-dev/pandas/issues/27585),
    [GH 27840](https://github.com/pandas-dev/pandas/issues/27840))
  id: totrans-25
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 解决了涉及`Index`和其他索引类的循环引用，以允许在不运行GC的情况下对索引对象进行垃圾回收。 ([GH 27585](https://github.com/pandas-dev/pandas/issues/27585),
    [GH 27840](https://github.com/pandas-dev/pandas/issues/27840))
- en: Fix regression in assigning values to a single column of a DataFrame with a
    `MultiIndex` columns ([GH 27841](https://github.com/pandas-dev/pandas/issues/27841)).
  id: totrans-26
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了将值分配给DataFrame的单个列时出现的回归问题，该DataFrame具有`MultiIndex`列 ([GH 27841](https://github.com/pandas-dev/pandas/issues/27841)).
- en: Fix regression in `.ix` fallback with an `IntervalIndex` ([GH 27865](https://github.com/pandas-dev/pandas/issues/27865)).
  id: totrans-27
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了带有`IntervalIndex`的`.ix`回退中的回归问题 ([GH 27865](https://github.com/pandas-dev/pandas/issues/27865)).
- en: Missing
  id: totrans-28
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 缺失
- en: Bug in [`pandas.isnull()`](../reference/api/pandas.isnull.html#pandas.isnull
    "pandas.isnull") or [`pandas.isna()`](../reference/api/pandas.isna.html#pandas.isna
    "pandas.isna") when the input is a type e.g. `type(pandas.Series())` ([GH 27482](https://github.com/pandas-dev/pandas/issues/27482))
  id: totrans-29
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当输入是一种类型时，修复了[`pandas.isnull()`](../reference/api/pandas.isnull.html#pandas.isnull
    "pandas.isnull")或[`pandas.isna()`](../reference/api/pandas.isna.html#pandas.isna
    "pandas.isna")中的错误，例如`type(pandas.Series())` ([GH 27482](https://github.com/pandas-dev/pandas/issues/27482))
- en: IO
  id: totrans-30
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 输入/输出
- en: Avoid calling `S3File.s3` when reading parquet, as this was removed in s3fs
    version 0.3.0 ([GH 27756](https://github.com/pandas-dev/pandas/issues/27756))
  id: totrans-31
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在读取parquet时避免调用`S3File.s3`，因为这在s3fs版本0.3.0中已被删除 ([GH 27756](https://github.com/pandas-dev/pandas/issues/27756))
- en: Better error message when a negative header is passed in [`pandas.read_csv()`](../reference/api/pandas.read_csv.html#pandas.read_csv
    "pandas.read_csv") ([GH 27779](https://github.com/pandas-dev/pandas/issues/27779))
  id: totrans-32
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当在[`pandas.read_csv()`](../reference/api/pandas.read_csv.html#pandas.read_csv
    "pandas.read_csv")中传递负标头时，改进了错误消息 ([GH 27779](https://github.com/pandas-dev/pandas/issues/27779))
- en: Follow the `min_rows` display option (introduced in v0.25.0) correctly in the
    HTML repr in the notebook ([GH 27991](https://github.com/pandas-dev/pandas/issues/27991)).
  id: totrans-33
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在笔记本中的HTML repr中正确地遵循`min_rows`显示选项（在v0.25.0中引入） ([GH 27991](https://github.com/pandas-dev/pandas/issues/27991)).
- en: Plotting
  id: totrans-34
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 绘图
- en: Added a `pandas_plotting_backends` entrypoint group for registering plot backends.
    See [Plotting backends](../development/extending.html#extending-plotting-backends)
    for more ([GH 26747](https://github.com/pandas-dev/pandas/issues/26747)).
  id: totrans-35
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 添加了一个`pandas_plotting_backends`入口点组，用于注册绘图后端。有关更多信息，请参见[Plotting backends](../development/extending.html#extending-plotting-backends)
    ([GH 26747](https://github.com/pandas-dev/pandas/issues/26747)).
- en: Fixed the re-instatement of Matplotlib datetime converters after calling [`pandas.plotting.deregister_matplotlib_converters()`](../reference/api/pandas.plotting.deregister_matplotlib_converters.html#pandas.plotting.deregister_matplotlib_converters
    "pandas.plotting.deregister_matplotlib_converters") ([GH 27481](https://github.com/pandas-dev/pandas/issues/27481)).
  id: totrans-36
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在调用[`pandas.plotting.deregister_matplotlib_converters()`](../reference/api/pandas.plotting.deregister_matplotlib_converters.html#pandas.plotting.deregister_matplotlib_converters
    "pandas.plotting.deregister_matplotlib_converters")后，修复了重新设置Matplotlib日期时间转换器的问题
    ([GH 27481](https://github.com/pandas-dev/pandas/issues/27481)).
- en: Fix compatibility issue with matplotlib when passing a pandas `Index` to a plot
    call ([GH 27775](https://github.com/pandas-dev/pandas/issues/27775)).
  id: totrans-37
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了将pandas的`Index`传递给绘图调用时与matplotlib兼容性问题 ([GH 27775](https://github.com/pandas-dev/pandas/issues/27775)).
- en: GroupBy/resample/rolling
  id: totrans-38
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 分组/重采样/滚动
- en: Fixed regression in `pands.core.groupby.DataFrameGroupBy.quantile()` raising
    when multiple quantiles are given ([GH 27526](https://github.com/pandas-dev/pandas/issues/27526))
  id: totrans-39
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了当给定多个分位数时，`pands.core.groupby.DataFrameGroupBy.quantile()`中的回归问题 ([GH 27526](https://github.com/pandas-dev/pandas/issues/27526))
- en: Bug in [`DataFrameGroupBy.transform()`](../reference/api/pandas.core.groupby.DataFrameGroupBy.transform.html#pandas.core.groupby.DataFrameGroupBy.transform
    "pandas.core.groupby.DataFrameGroupBy.transform") where applying a timezone conversion
    lambda function would drop timezone information ([GH 27496](https://github.com/pandas-dev/pandas/issues/27496))
  id: totrans-40
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在 [`DataFrameGroupBy.transform()`](../reference/api/pandas.core.groupby.DataFrameGroupBy.transform.html#pandas.core.groupby.DataFrameGroupBy.transform
    "pandas.core.groupby.DataFrameGroupBy.transform") 中的错误，应用时区转换 lambda 函数会丢失时区信息（[GH
    27496](https://github.com/pandas-dev/pandas/issues/27496)）
- en: Bug in `GroupBy.nth()` where `observed=False` was being ignored for Categorical
    groupers ([GH 26385](https://github.com/pandas-dev/pandas/issues/26385))
  id: totrans-41
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在 `GroupBy.nth()` 中的错误，其中 `observed=False` 被忽略了，用于分类分组器（[GH 26385](https://github.com/pandas-dev/pandas/issues/26385)）
- en: Bug in windowing over read-only arrays ([GH 27766](https://github.com/pandas-dev/pandas/issues/27766))
  id: totrans-42
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在只读数组上窗口化的错误（[GH 27766](https://github.com/pandas-dev/pandas/issues/27766)）
- en: Fixed segfault in `.DataFrameGroupBy.quantile` when an invalid quantile was
    passed ([GH 27470](https://github.com/pandas-dev/pandas/issues/27470))
  id: totrans-43
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了当传递无效分位数时，在 `.DataFrameGroupBy.quantile` 中出现段错误的问题（[GH 27470](https://github.com/pandas-dev/pandas/issues/27470)）
- en: Reshaping
  id: totrans-44
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 重塑
- en: A `KeyError` is now raised if `.unstack()` is called on a `Series` or `DataFrame`
    with a flat `Index` passing a name which is not the correct one ([GH 18303](https://github.com/pandas-dev/pandas/issues/18303))
  id: totrans-45
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当在具有平坦 `Index` 的 `Series` 或 `DataFrame` 上调用 `.unstack()` 并传递一个不正确的名称时，现在会引发
    `KeyError` 错误（[GH 18303](https://github.com/pandas-dev/pandas/issues/18303)）
- en: Bug `merge_asof()` could not merge `Timedelta` objects when passing `tolerance`
    kwarg ([GH 27642](https://github.com/pandas-dev/pandas/issues/27642))
  id: totrans-46
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了在传递 `tolerance` 关键字参数时，`merge_asof()` 无法合并 `Timedelta` 对象的错误（[GH 27642](https://github.com/pandas-dev/pandas/issues/27642)）
- en: Bug in `DataFrame.crosstab()` when `margins` set to `True` and `normalize` is
    not `False`, an error is raised. ([GH 27500](https://github.com/pandas-dev/pandas/issues/27500))
  id: totrans-47
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在 `DataFrame.crosstab()` 中的错误，当 `margins` 设置为 `True` 且 `normalize` 不是 `False`
    时，会引发错误。（[GH 27500](https://github.com/pandas-dev/pandas/issues/27500)）
- en: '`DataFrame.join()` now suppresses the `FutureWarning` when the sort parameter
    is specified ([GH 21952](https://github.com/pandas-dev/pandas/issues/21952))'
  id: totrans-48
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当指定了排序参数时，`DataFrame.join()` 现在会抑制 `FutureWarning`（[GH 21952](https://github.com/pandas-dev/pandas/issues/21952)）
- en: Bug in `DataFrame.join()` raising with readonly arrays ([GH 27943](https://github.com/pandas-dev/pandas/issues/27943))
  id: totrans-49
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了在使用只读数组时引发错误的 `DataFrame.join()`（[GH 27943](https://github.com/pandas-dev/pandas/issues/27943)）
- en: Sparse
  id: totrans-50
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 稀疏
- en: Bug in reductions for `Series` with Sparse dtypes ([GH 27080](https://github.com/pandas-dev/pandas/issues/27080))
  id: totrans-51
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在具有稀疏数据类型的 `Series` 的缩减中的错误（[GH 27080](https://github.com/pandas-dev/pandas/issues/27080)）
- en: Other
  id: totrans-52
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 其他
- en: Bug in `Series.replace()` and `DataFrame.replace()` when replacing timezone-aware
    timestamps using a dict-like replacer ([GH 27720](https://github.com/pandas-dev/pandas/issues/27720))
  id: totrans-53
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在替换时区感知时间戳时，`Series.replace()` 和 `DataFrame.replace()` 中的错误，使用类似字典的替换器时，会丢失时区信息（[GH
    27720](https://github.com/pandas-dev/pandas/issues/27720)）
- en: 'Bug in `Series.rename()` when using a custom type indexer. Now any value that
    isn’t callable or dict-like is treated as a scalar. ([GH 27814](https://github.com/pandas-dev/pandas/issues/27814))  ##
    Contributors'
  id: totrans-54
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '修复了在使用自定义类型索引器时 `Series.rename()` 中的错误。现在，任何非可调用或类似字典的值都将被视为标量。（[GH 27814](https://github.com/pandas-dev/pandas/issues/27814)）  ##
    贡献者'
- en: A total of 5 people contributed patches to this release. People with a “+” by
    their names contributed a patch for the first time.
  id: totrans-55
  prefs: []
  type: TYPE_NORMAL
  zh: 总共有 5 人对此版本做出了贡献。带有 “+” 的人首次为此版本提供了补丁。
- en: Jeff Reback
  id: totrans-56
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 杰夫·里巴克
- en: Joris Van den Bossche
  id: totrans-57
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 乔里斯·范登博斯切
- en: MeeseeksMachine +
  id: totrans-58
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 米斯克斯机器 +
- en: Tom Augspurger
  id: totrans-59
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 汤姆·奥格斯伯格
- en: jbrockmendel
  id: totrans-60
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 杰布·罗克曼德尔
- en: IO and LZMA
  id: totrans-61
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: IO 和 LZMA
- en: Some users may unknowingly have an incomplete Python installation lacking the
    `lzma` module from the standard library. In this case, `import pandas` failed
    due to an `ImportError` ([GH 27575](https://github.com/pandas-dev/pandas/issues/27575)).
    pandas will now warn, rather than raising an `ImportError` if the `lzma` module
    is not present. Any subsequent attempt to use `lzma` methods will raise a `RuntimeError`.
    A possible fix for the lack of the `lzma` module is to ensure you have the necessary
    libraries and then re-install Python. For example, on MacOS installing Python
    with `pyenv` may lead to an incomplete Python installation due to unmet system
    dependencies at compilation time (like `xz`). Compilation will succeed, but Python
    might fail at run time. The issue can be solved by installing the necessary dependencies
    and then re-installing Python.
  id: totrans-62
  prefs: []
  type: TYPE_NORMAL
  zh: 一些用户可能在不知情的情况下，其 Python 安装不完整，缺少标准库中的 `lzma` 模块。在这种情况下，由于缺少 `lzma` 模块，`import
    pandas` 会失败导致 `ImportError`（[GH 27575](https://github.com/pandas-dev/pandas/issues/27575)）。现在
    pandas 将发出警告，而不是在 `lzma` 模块不存在时引发 `ImportError`。任何后续尝试使用 `lzma` 方法的操作都将引发 `RuntimeError`。解决缺少
    `lzma` 模块的一个可能方法是确保您拥有必要的库，然后重新安装 Python。例如，在 MacOS 上使用 `pyenv` 安装 Python 可能会导致由于编译时未满足的系统依赖项（如
    `xz`）而导致 Python 安装不完整。编译将成功，但 Python 可能在运行时失败。该问题可通过安装必要的依赖项然后重新安装 Python 来解决。
- en: '## Bug fixes'
  id: totrans-63
  prefs: []
  type: TYPE_NORMAL
  zh: '## 修复的错误'
- en: Categorical
  id: totrans-64
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 分类
- en: Bug in `Categorical.fillna()` that would replace all values, not just those
    that are `NaN` ([GH 26215](https://github.com/pandas-dev/pandas/issues/26215))
  id: totrans-65
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`Categorical.fillna()` 中的错误，会替换所有值，而不仅仅是那些是 `NaN` 的值（[GH 26215](https://github.com/pandas-dev/pandas/issues/26215)）'
- en: Datetimelike
  id: totrans-66
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 日期时间类
- en: Bug in `to_datetime()` where passing a timezone-naive `DatetimeArray` or `DatetimeIndex`
    and `utc=True` would incorrectly return a timezone-naive result ([GH 27733](https://github.com/pandas-dev/pandas/issues/27733))
  id: totrans-67
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`to_datetime()` 中的错误，当传递一个时区不明确的 `DatetimeArray` 或 `DatetimeIndex` 以及 `utc=True`
    时，将错误地返回一个时区不明确的结果（[GH 27733](https://github.com/pandas-dev/pandas/issues/27733)）'
- en: Bug in `Period.to_timestamp()` where a `Period` outside the `Timestamp` implementation
    bounds (roughly 1677-09-21 to 2262-04-11) would return an incorrect `Timestamp`
    instead of raising `OutOfBoundsDatetime` ([GH 19643](https://github.com/pandas-dev/pandas/issues/19643))
  id: totrans-68
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在 `Period.to_timestamp()` 中的错误，当 `Period` 超出 `Timestamp` 实现范围（大约为 1677-09-21
    至 2262-04-11）时，将返回一个不正确的 `Timestamp`，而不是引发 `OutOfBoundsDatetime`（[GH 19643](https://github.com/pandas-dev/pandas/issues/19643)）
- en: Bug in iterating over `DatetimeIndex` when the underlying data is read-only
    ([GH 28055](https://github.com/pandas-dev/pandas/issues/28055))
  id: totrans-69
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在迭代 `DatetimeIndex` 时，当底层数据是只读时会发生错误（[GH 28055](https://github.com/pandas-dev/pandas/issues/28055)）
- en: Timezones
  id: totrans-70
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 时区
- en: Bug in `Index` where a numpy object array with a timezone aware `Timestamp`
    and `np.nan` would not return a `DatetimeIndex` ([GH 27011](https://github.com/pandas-dev/pandas/issues/27011))
  id: totrans-71
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在具有时区感知的 `Timestamp` 和 `np.nan` 的 numpy 对象数组中，`Index` 中的错误，不会返回 `DatetimeIndex`（[GH
    27011](https://github.com/pandas-dev/pandas/issues/27011)）
- en: Numeric
  id: totrans-72
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 数值
- en: Bug in `Series.interpolate()` when using a timezone aware `DatetimeIndex` ([GH
    27548](https://github.com/pandas-dev/pandas/issues/27548))
  id: totrans-73
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在使用带有时区感知的 `DatetimeIndex` 时，`Series.interpolate()` 中的错误（[GH 27548](https://github.com/pandas-dev/pandas/issues/27548)）
- en: Bug when printing negative floating point complex numbers would raise an `IndexError`
    ([GH 27484](https://github.com/pandas-dev/pandas/issues/27484))
  id: totrans-74
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 打印负浮点复数时会引发 `IndexError` 的错误（[GH 27484](https://github.com/pandas-dev/pandas/issues/27484)）
- en: Bug where `DataFrame` arithmetic operators such as `DataFrame.mul()` with a
    `Series` with axis=1 would raise an `AttributeError` on `DataFrame` larger than
    the minimum threshold to invoke numexpr ([GH 27636](https://github.com/pandas-dev/pandas/issues/27636))
  id: totrans-75
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当 `DataFrame` 算术运算符（例如具有轴=1 的 `DataFrame.mul()`）与超过调用 numexpr 的最小阈值的 `DataFrame`
    一起使用时，会在 `DataFrame` 上引发 `AttributeError`（[GH 27636](https://github.com/pandas-dev/pandas/issues/27636)）
- en: Bug in `DataFrame` arithmetic where missing values in results were incorrectly
    masked with `NaN` instead of `Inf` ([GH 27464](https://github.com/pandas-dev/pandas/issues/27464))
  id: totrans-76
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`DataFrame` 算术中的错误，结果中的缺失值被错误地掩盖为 `NaN` 而不是 `Inf`（[GH 27464](https://github.com/pandas-dev/pandas/issues/27464)）'
- en: Conversion
  id: totrans-77
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 转换
- en: Improved the warnings for the deprecated methods `Series.real()` and `Series.imag()`
    ([GH 27610](https://github.com/pandas-dev/pandas/issues/27610))
  id: totrans-78
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 改进了对已弃用方法 `Series.real()` 和 `Series.imag()` 的警告（[GH 27610](https://github.com/pandas-dev/pandas/issues/27610)）
- en: Interval
  id: totrans-79
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 区间
- en: Bug in `IntervalIndex` where `dir(obj)` would raise `ValueError` ([GH 27571](https://github.com/pandas-dev/pandas/issues/27571))
  id: totrans-80
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`IntervalIndex` 中的错误，`dir(obj)` 会引发 `ValueError`（[GH 27571](https://github.com/pandas-dev/pandas/issues/27571)）'
- en: Indexing
  id: totrans-81
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 索引
- en: Bug in partial-string indexing returning a NumPy array rather than a `Series`
    when indexing with a scalar like `.loc['2015']` ([GH 27516](https://github.com/pandas-dev/pandas/issues/27516))
  id: totrans-82
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在部分字符串索引中存在的错误，当使用像`.loc['2015']`这样的标量索引时，返回的是 NumPy 数组而不是`Series`（[GH 27516](https://github.com/pandas-dev/pandas/issues/27516)）。
- en: Break reference cycle involving `Index` and other index classes to allow garbage
    collection of index objects without running the GC. ([GH 27585](https://github.com/pandas-dev/pandas/issues/27585),
    [GH 27840](https://github.com/pandas-dev/pandas/issues/27840))
  id: totrans-83
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 打破涉及`Index`和其他索引类的循环引用，以允许索引对象的垃圾回收而不运行 GC。（[GH 27585](https://github.com/pandas-dev/pandas/issues/27585)，[GH
    27840](https://github.com/pandas-dev/pandas/issues/27840)）
- en: Fix regression in assigning values to a single column of a DataFrame with a
    `MultiIndex` columns ([GH 27841](https://github.com/pandas-dev/pandas/issues/27841)).
  id: totrans-84
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了在具有`MultiIndex`列的 DataFrame 的单列分配值时的回归问题（[GH 27841](https://github.com/pandas-dev/pandas/issues/27841)）。
- en: Fix regression in `.ix` fallback with an `IntervalIndex` ([GH 27865](https://github.com/pandas-dev/pandas/issues/27865)).
  id: totrans-85
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了`.ix`回退中的回归问题，带有`IntervalIndex`（[GH 27865](https://github.com/pandas-dev/pandas/issues/27865)）。
- en: Missing
  id: totrans-86
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 缺失
- en: Bug in [`pandas.isnull()`](../reference/api/pandas.isnull.html#pandas.isnull
    "pandas.isnull") or [`pandas.isna()`](../reference/api/pandas.isna.html#pandas.isna
    "pandas.isna") when the input is a type e.g. `type(pandas.Series())` ([GH 27482](https://github.com/pandas-dev/pandas/issues/27482))
  id: totrans-87
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当输入为类型时，[`pandas.isnull()`](../reference/api/pandas.isnull.html#pandas.isnull
    "pandas.isnull")或[`pandas.isna()`](../reference/api/pandas.isna.html#pandas.isna
    "pandas.isna")中的错误，例如`type(pandas.Series())`（[GH 27482](https://github.com/pandas-dev/pandas/issues/27482)）。
- en: IO
  id: totrans-88
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: IO
- en: Avoid calling `S3File.s3` when reading parquet, as this was removed in s3fs
    version 0.3.0 ([GH 27756](https://github.com/pandas-dev/pandas/issues/27756))
  id: totrans-89
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在读取 Parquet 时避免调用`S3File.s3`，因为这在 s3fs 版本 0.3.0 中已被移除（[GH 27756](https://github.com/pandas-dev/pandas/issues/27756)）。
- en: Better error message when a negative header is passed in [`pandas.read_csv()`](../reference/api/pandas.read_csv.html#pandas.read_csv
    "pandas.read_csv") ([GH 27779](https://github.com/pandas-dev/pandas/issues/27779))
  id: totrans-90
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当在[`pandas.read_csv()`](../reference/api/pandas.read_csv.html#pandas.read_csv
    "pandas.read_csv")中传递负标题时，改进了错误消息（[GH 27779](https://github.com/pandas-dev/pandas/issues/27779)）。
- en: Follow the `min_rows` display option (introduced in v0.25.0) correctly in the
    HTML repr in the notebook ([GH 27991](https://github.com/pandas-dev/pandas/issues/27991)).
  id: totrans-91
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在笔记本中的 HTML 表示中正确地遵循`min_rows`显示选项（在 v0.25.0 中引入）（[GH 27991](https://github.com/pandas-dev/pandas/issues/27991)）。
- en: Plotting
  id: totrans-92
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 绘图
- en: Added a `pandas_plotting_backends` entrypoint group for registering plot backends.
    See [Plotting backends](../development/extending.html#extending-plotting-backends)
    for more ([GH 26747](https://github.com/pandas-dev/pandas/issues/26747)).
  id: totrans-93
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 添加了一个`pandas_plotting_backends`入口点组，用于注册绘图后端。详见[绘图后端](../development/extending.html#extending-plotting-backends)以获取更多信息（[GH
    26747](https://github.com/pandas-dev/pandas/issues/26747)）。
- en: Fixed the re-instatement of Matplotlib datetime converters after calling [`pandas.plotting.deregister_matplotlib_converters()`](../reference/api/pandas.plotting.deregister_matplotlib_converters.html#pandas.plotting.deregister_matplotlib_converters
    "pandas.plotting.deregister_matplotlib_converters") ([GH 27481](https://github.com/pandas-dev/pandas/issues/27481)).
  id: totrans-94
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在调用[`pandas.plotting.deregister_matplotlib_converters()`](../reference/api/pandas.plotting.deregister_matplotlib_converters.html#pandas.plotting.deregister_matplotlib_converters
    "pandas.plotting.deregister_matplotlib_converters")后重新安装 Matplotlib 日期时间转换器的错误（[GH
    27481](https://github.com/pandas-dev/pandas/issues/27481)）。
- en: Fix compatibility issue with matplotlib when passing a pandas `Index` to a plot
    call ([GH 27775](https://github.com/pandas-dev/pandas/issues/27775)).
  id: totrans-95
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了将 pandas `Index`传递给绘图调用时与 matplotlib 的兼容性问题（[GH 27775](https://github.com/pandas-dev/pandas/issues/27775)）。
- en: GroupBy/resample/rolling
  id: totrans-96
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: GroupBy/resample/rolling
- en: Fixed regression in `pands.core.groupby.DataFrameGroupBy.quantile()` raising
    when multiple quantiles are given ([GH 27526](https://github.com/pandas-dev/pandas/issues/27526))
  id: totrans-97
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了在给定多个分位数时，`pands.core.groupby.DataFrameGroupBy.quantile()`中引发的回归问题（[GH 27526](https://github.com/pandas-dev/pandas/issues/27526)）。
- en: Bug in [`DataFrameGroupBy.transform()`](../reference/api/pandas.core.groupby.DataFrameGroupBy.transform.html#pandas.core.groupby.DataFrameGroupBy.transform
    "pandas.core.groupby.DataFrameGroupBy.transform") where applying a timezone conversion
    lambda function would drop timezone information ([GH 27496](https://github.com/pandas-dev/pandas/issues/27496))
  id: totrans-98
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在[`DataFrameGroupBy.transform()`](../reference/api/pandas.core.groupby.DataFrameGroupBy.transform.html#pandas.core.groupby.DataFrameGroupBy.transform
    "pandas.core.groupby.DataFrameGroupBy.transform")中存在的错误，在应用时，时区转换 lambda 函数会丢失时区信息（[GH
    27496](https://github.com/pandas-dev/pandas/issues/27496)）。
- en: Bug in `GroupBy.nth()` where `observed=False` was being ignored for Categorical
    groupers ([GH 26385](https://github.com/pandas-dev/pandas/issues/26385))
  id: totrans-99
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在`GroupBy.nth()`中存在的问题，即对分类分组器忽略了`observed=False`（[GH 26385](https://github.com/pandas-dev/pandas/issues/26385)）修复。
- en: Bug in windowing over read-only arrays ([GH 27766](https://github.com/pandas-dev/pandas/issues/27766))
  id: totrans-100
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在只读数组上的窗口处理存在错误（[GH 27766](https://github.com/pandas-dev/pandas/issues/27766)）。
- en: Fixed segfault in `.DataFrameGroupBy.quantile` when an invalid quantile was
    passed ([GH 27470](https://github.com/pandas-dev/pandas/issues/27470))
  id: totrans-101
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当传递无效分位数时，在`.DataFrameGroupBy.quantile`中的段错误已修复（[GH 27470](https://github.com/pandas-dev/pandas/issues/27470)）。
- en: Reshaping
  id: totrans-102
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 重塑
- en: A `KeyError` is now raised if `.unstack()` is called on a `Series` or `DataFrame`
    with a flat `Index` passing a name which is not the correct one ([GH 18303](https://github.com/pandas-dev/pandas/issues/18303))
  id: totrans-103
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 现在，如果对具有平坦`Index`的`Series`或`DataFrame`调用`.unstack()`，并传递的名称不是正确的名称，则会引发`KeyError`（[GH
    18303](https://github.com/pandas-dev/pandas/issues/18303)）。
- en: Bug `merge_asof()` could not merge `Timedelta` objects when passing `tolerance`
    kwarg ([GH 27642](https://github.com/pandas-dev/pandas/issues/27642))
  id: totrans-104
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在传递`tolerance`关键字参数时，`merge_asof()`无法合并`Timedelta`对象的错误已修复（[GH 27642](https://github.com/pandas-dev/pandas/issues/27642)）。
- en: Bug in `DataFrame.crosstab()` when `margins` set to `True` and `normalize` is
    not `False`, an error is raised. ([GH 27500](https://github.com/pandas-dev/pandas/issues/27500))
  id: totrans-105
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当`DataFrame.crosstab()`中`margins`设置为`True`并且`normalize`不是`False`时，会引发错误（[GH
    27500](https://github.com/pandas-dev/pandas/issues/27500)）已修复。
- en: '`DataFrame.join()` now suppresses the `FutureWarning` when the sort parameter
    is specified ([GH 21952](https://github.com/pandas-dev/pandas/issues/21952))'
  id: totrans-106
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当指定了排序参数时，`DataFrame.join()`现在抑制了`FutureWarning`（[GH 21952](https://github.com/pandas-dev/pandas/issues/21952)）。
- en: Bug in `DataFrame.join()` raising with readonly arrays ([GH 27943](https://github.com/pandas-dev/pandas/issues/27943))
  id: totrans-107
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在使用只读数组时，`DataFrame.join()`中引发的错误已修复（[GH 27943](https://github.com/pandas-dev/pandas/issues/27943)）。
- en: Sparse
  id: totrans-108
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 稀疏
- en: Bug in reductions for `Series` with Sparse dtypes ([GH 27080](https://github.com/pandas-dev/pandas/issues/27080))
  id: totrans-109
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 对于具有稀疏dtype的`Series`的缩减存在错误（[GH 27080](https://github.com/pandas-dev/pandas/issues/27080)）。
- en: Other
  id: totrans-110
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 其他
- en: Bug in `Series.replace()` and `DataFrame.replace()` when replacing timezone-aware
    timestamps using a dict-like replacer ([GH 27720](https://github.com/pandas-dev/pandas/issues/27720))
  id: totrans-111
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在使用字典形式的替换器替换时区感知时间戳时，`Series.replace()`和`DataFrame.replace()`中的错误已修复（[GH 27720](https://github.com/pandas-dev/pandas/issues/27720)）。
- en: Bug in `Series.rename()` when using a custom type indexer. Now any value that
    isn’t callable or dict-like is treated as a scalar. ([GH 27814](https://github.com/pandas-dev/pandas/issues/27814))
  id: totrans-112
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在使用自定义类型索引器时的`Series.rename()`中的错误修复。现在，任何不可调用或类似于字典的值都被视为标量（[GH 27814](https://github.com/pandas-dev/pandas/issues/27814)）。
- en: Categorical
  id: totrans-113
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 分类
- en: Bug in `Categorical.fillna()` that would replace all values, not just those
    that are `NaN` ([GH 26215](https://github.com/pandas-dev/pandas/issues/26215))
  id: totrans-114
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在`Categorical.fillna()`中的错误修复，即替换所有值，而不仅仅是`NaN`（[GH 26215](https://github.com/pandas-dev/pandas/issues/26215)）。
- en: Datetimelike
  id: totrans-115
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 日期时间样式
- en: Bug in `to_datetime()` where passing a timezone-naive `DatetimeArray` or `DatetimeIndex`
    and `utc=True` would incorrectly return a timezone-naive result ([GH 27733](https://github.com/pandas-dev/pandas/issues/27733))
  id: totrans-116
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在`to_datetime()`中的错误修复，即传递时区非敏感的`DatetimeArray`或`DatetimeIndex`和`utc=True`时将错误地返回时区非敏感的结果（[GH
    27733](https://github.com/pandas-dev/pandas/issues/27733)）。
- en: Bug in `Period.to_timestamp()` where a `Period` outside the `Timestamp` implementation
    bounds (roughly 1677-09-21 to 2262-04-11) would return an incorrect `Timestamp`
    instead of raising `OutOfBoundsDatetime` ([GH 19643](https://github.com/pandas-dev/pandas/issues/19643))
  id: totrans-117
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在`Period.to_timestamp()`中的错误修复，即`Period`超出`Timestamp`实现范围（大约1677-09-21至2262-04-11）将返回一个不正确的`Timestamp`，而不是引发`OutOfBoundsDatetime`（[GH
    19643](https://github.com/pandas-dev/pandas/issues/19643)）。
- en: Bug in iterating over `DatetimeIndex` when the underlying data is read-only
    ([GH 28055](https://github.com/pandas-dev/pandas/issues/28055))
  id: totrans-118
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当底层数据为只读时，迭代`DatetimeIndex`中的错误（[GH 28055](https://github.com/pandas-dev/pandas/issues/28055)）修复。
- en: Timezones
  id: totrans-119
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 时区
- en: Bug in `Index` where a numpy object array with a timezone aware `Timestamp`
    and `np.nan` would not return a `DatetimeIndex` ([GH 27011](https://github.com/pandas-dev/pandas/issues/27011))
  id: totrans-120
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在一个numpy对象数组中，具有时区感知的`Timestamp`和`np.nan`的`Index`不会返回`DatetimeIndex`的错误修复（[GH
    27011](https://github.com/pandas-dev/pandas/issues/27011)）。
- en: Numeric
  id: totrans-121
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 数值
- en: Bug in `Series.interpolate()` when using a timezone aware `DatetimeIndex` ([GH
    27548](https://github.com/pandas-dev/pandas/issues/27548))
  id: totrans-122
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在使用时区感知的`DatetimeIndex`时，`Series.interpolate()`中的错误修复（[GH 27548](https://github.com/pandas-dev/pandas/issues/27548)）。
- en: Bug when printing negative floating point complex numbers would raise an `IndexError`
    ([GH 27484](https://github.com/pandas-dev/pandas/issues/27484))
  id: totrans-123
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当打印负浮点复数时会引发 `IndexError` 的错误 ([GH 27484](https://github.com/pandas-dev/pandas/issues/27484))
- en: Bug where `DataFrame` arithmetic operators such as `DataFrame.mul()` with a
    `Series` with axis=1 would raise an `AttributeError` on `DataFrame` larger than
    the minimum threshold to invoke numexpr ([GH 27636](https://github.com/pandas-dev/pandas/issues/27636))
  id: totrans-124
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`DataFrame` 算术运算符的错误，例如带有轴=1的`DataFrame.mul()`与大于调用 numexpr 的最小阈值的`DataFrame`一起会在`DataFrame`上引发`AttributeError`
    ([GH 27636](https://github.com/pandas-dev/pandas/issues/27636))'
- en: Bug in `DataFrame` arithmetic where missing values in results were incorrectly
    masked with `NaN` instead of `Inf` ([GH 27464](https://github.com/pandas-dev/pandas/issues/27464))
  id: totrans-125
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 数据框架`DataFrame`算术中的错误，结果中的缺失值被错误地掩盖为`NaN`而不是`Inf`（[GH 27464](https://github.com/pandas-dev/pandas/issues/27464)）
- en: Conversion
  id: totrans-126
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 转换
- en: Improved the warnings for the deprecated methods `Series.real()` and `Series.imag()`
    ([GH 27610](https://github.com/pandas-dev/pandas/issues/27610))
  id: totrans-127
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 改进了对废弃方法`Series.real()`和`Series.imag()`的警告 ([GH 27610](https://github.com/pandas-dev/pandas/issues/27610))
- en: Interval
  id: totrans-128
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 区间
- en: Bug in `IntervalIndex` where `dir(obj)` would raise `ValueError` ([GH 27571](https://github.com/pandas-dev/pandas/issues/27571))
  id: totrans-129
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`IntervalIndex` 中的错误，`dir(obj)` 会引发 `ValueError` ([GH 27571](https://github.com/pandas-dev/pandas/issues/27571))'
- en: Indexing
  id: totrans-130
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 索引
- en: Bug in partial-string indexing returning a NumPy array rather than a `Series`
    when indexing with a scalar like `.loc['2015']` ([GH 27516](https://github.com/pandas-dev/pandas/issues/27516))
  id: totrans-131
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当使用标量进行索引时，部分字符串索引返回 NumPy 数组而不是 `Series` 的错误，例如`.loc['2015']` ([GH 27516](https://github.com/pandas-dev/pandas/issues/27516))
- en: Break reference cycle involving `Index` and other index classes to allow garbage
    collection of index objects without running the GC. ([GH 27585](https://github.com/pandas-dev/pandas/issues/27585),
    [GH 27840](https://github.com/pandas-dev/pandas/issues/27840))
  id: totrans-132
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 打破涉及 `Index` 和其他索引类的引用循环，以允许索引对象在不运行 GC 的情况下进行垃圾回收。 ([GH 27585](https://github.com/pandas-dev/pandas/issues/27585),
    [GH 27840](https://github.com/pandas-dev/pandas/issues/27840))
- en: Fix regression in assigning values to a single column of a DataFrame with a
    `MultiIndex` columns ([GH 27841](https://github.com/pandas-dev/pandas/issues/27841)).
  id: totrans-133
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了向具有`MultiIndex`列的 DataFrame 的单列分配值时的回归问题 ([GH 27841](https://github.com/pandas-dev/pandas/issues/27841))
- en: Fix regression in `.ix` fallback with an `IntervalIndex` ([GH 27865](https://github.com/pandas-dev/pandas/issues/27865)).
  id: totrans-134
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了`.ix`回退中的回归问题，使用了一个`IntervalIndex` ([GH 27865](https://github.com/pandas-dev/pandas/issues/27865))
- en: Missing
  id: totrans-135
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 缺失
- en: Bug in [`pandas.isnull()`](../reference/api/pandas.isnull.html#pandas.isnull
    "pandas.isnull") or [`pandas.isna()`](../reference/api/pandas.isna.html#pandas.isna
    "pandas.isna") when the input is a type e.g. `type(pandas.Series())` ([GH 27482](https://github.com/pandas-dev/pandas/issues/27482))
  id: totrans-136
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当输入为类型（例如`type(pandas.Series())`）时，[`pandas.isnull()`](../reference/api/pandas.isnull.html#pandas.isnull
    "pandas.isnull") 或 [`pandas.isna()`](../reference/api/pandas.isna.html#pandas.isna
    "pandas.isna") 中的错误 ([GH 27482](https://github.com/pandas-dev/pandas/issues/27482))
- en: IO
  id: totrans-137
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: IO
- en: Avoid calling `S3File.s3` when reading parquet, as this was removed in s3fs
    version 0.3.0 ([GH 27756](https://github.com/pandas-dev/pandas/issues/27756))
  id: totrans-138
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在读取 Parquet 时避免调用 `S3File.s3`，因为这在 s3fs 版本 0.3.0 中已被移除 ([GH 27756](https://github.com/pandas-dev/pandas/issues/27756))
- en: Better error message when a negative header is passed in [`pandas.read_csv()`](../reference/api/pandas.read_csv.html#pandas.read_csv
    "pandas.read_csv") ([GH 27779](https://github.com/pandas-dev/pandas/issues/27779))
  id: totrans-139
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当负标头传递给 [`pandas.read_csv()`](../reference/api/pandas.read_csv.html#pandas.read_csv
    "pandas.read_csv") 时，提供更好的错误消息 ([GH 27779](https://github.com/pandas-dev/pandas/issues/27779))
- en: Follow the `min_rows` display option (introduced in v0.25.0) correctly in the
    HTML repr in the notebook ([GH 27991](https://github.com/pandas-dev/pandas/issues/27991)).
  id: totrans-140
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在笔记本中的 HTML 表示中正确遵循 `min_rows` 显示选项（v0.25.0 中引入） ([GH 27991](https://github.com/pandas-dev/pandas/issues/27991))
- en: Plotting
  id: totrans-141
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 绘图
- en: Added a `pandas_plotting_backends` entrypoint group for registering plot backends.
    See [Plotting backends](../development/extending.html#extending-plotting-backends)
    for more ([GH 26747](https://github.com/pandas-dev/pandas/issues/26747)).
  id: totrans-142
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 添加了一个`pandas_plotting_backends`入口点组，用于注册绘图后端。有关更多信息，请参见 [Plotting backends](../development/extending.html#extending-plotting-backends)
    ([GH 26747](https://github.com/pandas-dev/pandas/issues/26747))
- en: Fixed the re-instatement of Matplotlib datetime converters after calling [`pandas.plotting.deregister_matplotlib_converters()`](../reference/api/pandas.plotting.deregister_matplotlib_converters.html#pandas.plotting.deregister_matplotlib_converters
    "pandas.plotting.deregister_matplotlib_converters") ([GH 27481](https://github.com/pandas-dev/pandas/issues/27481)).
  id: totrans-143
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在调用[`pandas.plotting.deregister_matplotlib_converters()`](../reference/api/pandas.plotting.deregister_matplotlib_converters.html#pandas.plotting.deregister_matplotlib_converters
    "pandas.plotting.deregister_matplotlib_converters")后，重新启用 Matplotlib 日期时间转换器（[GH
    27481](https://github.com/pandas-dev/pandas/issues/27481)）。
- en: Fix compatibility issue with matplotlib when passing a pandas `Index` to a plot
    call ([GH 27775](https://github.com/pandas-dev/pandas/issues/27775)).
  id: totrans-144
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了与 matplotlib 兼容性问题，当将 pandas `Index` 传递给绘图调用时（[GH 27775](https://github.com/pandas-dev/pandas/issues/27775)）
- en: GroupBy/resample/rolling
  id: totrans-145
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: GroupBy/resample/rolling
- en: Fixed regression in `pands.core.groupby.DataFrameGroupBy.quantile()` raising
    when multiple quantiles are given ([GH 27526](https://github.com/pandas-dev/pandas/issues/27526))
  id: totrans-146
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 修复了`pands.core.groupby.DataFrameGroupBy.quantile()` 中的回归，当给出多个分位数时会引发异常（[GH
    27526](https://github.com/pandas-dev/pandas/issues/27526)）
- en: Bug in [`DataFrameGroupBy.transform()`](../reference/api/pandas.core.groupby.DataFrameGroupBy.transform.html#pandas.core.groupby.DataFrameGroupBy.transform
    "pandas.core.groupby.DataFrameGroupBy.transform") where applying a timezone conversion
    lambda function would drop timezone information ([GH 27496](https://github.com/pandas-dev/pandas/issues/27496))
  id: totrans-147
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在[`DataFrameGroupBy.transform()`](../reference/api/pandas.core.groupby.DataFrameGroupBy.transform.html#pandas.core.groupby.DataFrameGroupBy.transform
    "pandas.core.groupby.DataFrameGroupBy.transform") 中存在 bug，在应用时区转换 lambda 函数时会丢失时区信息（[GH
    27496](https://github.com/pandas-dev/pandas/issues/27496)）
- en: Bug in `GroupBy.nth()` where `observed=False` was being ignored for Categorical
    groupers ([GH 26385](https://github.com/pandas-dev/pandas/issues/26385))
  id: totrans-148
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`GroupBy.nth()` 中存在 bug，当 `observed=False` 时，分类分组器被忽略（[GH 26385](https://github.com/pandas-dev/pandas/issues/26385)）'
- en: Bug in windowing over read-only arrays ([GH 27766](https://github.com/pandas-dev/pandas/issues/27766))
  id: totrans-149
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在只读数组上进行窗口操作中存在 bug（[GH 27766](https://github.com/pandas-dev/pandas/issues/27766)）
- en: Fixed segfault in `.DataFrameGroupBy.quantile` when an invalid quantile was
    passed ([GH 27470](https://github.com/pandas-dev/pandas/issues/27470))
  id: totrans-150
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当传递无效分位数时，修复了`.DataFrameGroupBy.quantile` 中的段错误（[GH 27470](https://github.com/pandas-dev/pandas/issues/27470)）
- en: Reshaping
  id: totrans-151
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 重塑
- en: A `KeyError` is now raised if `.unstack()` is called on a `Series` or `DataFrame`
    with a flat `Index` passing a name which is not the correct one ([GH 18303](https://github.com/pandas-dev/pandas/issues/18303))
  id: totrans-152
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 如果在平坦的 `Index` 上调用 `.unstack()`，并传递一个不正确的名称，则现在会引发 `KeyError`，无论是 `Series` 还是
    `DataFrame`（[GH 18303](https://github.com/pandas-dev/pandas/issues/18303)）
- en: Bug `merge_asof()` could not merge `Timedelta` objects when passing `tolerance`
    kwarg ([GH 27642](https://github.com/pandas-dev/pandas/issues/27642))
  id: totrans-153
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在调用 `tolerance` kwarg 时，`merge_asof()` 中存在 bug，无法合并 `Timedelta` 对象（[GH 27642](https://github.com/pandas-dev/pandas/issues/27642)）
- en: Bug in `DataFrame.crosstab()` when `margins` set to `True` and `normalize` is
    not `False`, an error is raised. ([GH 27500](https://github.com/pandas-dev/pandas/issues/27500))
  id: totrans-154
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在`DataFrame.crosstab()` 中存在 bug，当 `margins` 设置为 `True` 且 `normalize` 不为 `False`
    时，会引发错误（[GH 27500](https://github.com/pandas-dev/pandas/issues/27500)）
- en: '`DataFrame.join()` now suppresses the `FutureWarning` when the sort parameter
    is specified ([GH 21952](https://github.com/pandas-dev/pandas/issues/21952))'
  id: totrans-155
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当指定 sort 参数时，`DataFrame.join()` 现在会抑制 `FutureWarning`（[GH 21952](https://github.com/pandas-dev/pandas/issues/21952)）
- en: Bug in `DataFrame.join()` raising with readonly arrays ([GH 27943](https://github.com/pandas-dev/pandas/issues/27943))
  id: totrans-156
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`DataFrame.join()` 中存在 bug，会因为只读数组而引发异常（[GH 27943](https://github.com/pandas-dev/pandas/issues/27943)）'
- en: Sparse
  id: totrans-157
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 稀疏
- en: Bug in reductions for `Series` with Sparse dtypes ([GH 27080](https://github.com/pandas-dev/pandas/issues/27080))
  id: totrans-158
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '`Series` 的稀疏类型在减少时存在 bug（[GH 27080](https://github.com/pandas-dev/pandas/issues/27080)）'
- en: Other
  id: totrans-159
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 其他
- en: Bug in `Series.replace()` and `DataFrame.replace()` when replacing timezone-aware
    timestamps using a dict-like replacer ([GH 27720](https://github.com/pandas-dev/pandas/issues/27720))
  id: totrans-160
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 在用字典样式替换器替换时，`Series.replace()` 和 `DataFrame.replace()` 中存在 bug，当替换带有时区信息的时间戳时（[GH
    27720](https://github.com/pandas-dev/pandas/issues/27720)）
- en: Bug in `Series.rename()` when using a custom type indexer. Now any value that
    isn’t callable or dict-like is treated as a scalar. ([GH 27814](https://github.com/pandas-dev/pandas/issues/27814))
  id: totrans-161
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 当使用自定义类型索引器时，`Series.rename()` 中存在 bug。现在，任何非可调用或类似字典的值都被视为标量（[GH 27814](https://github.com/pandas-dev/pandas/issues/27814)）
- en: '## Contributors'
  id: totrans-162
  prefs: []
  type: TYPE_NORMAL
  zh: '## 贡献者'
- en: A total of 5 people contributed patches to this release. People with a “+” by
    their names contributed a patch for the first time.
  id: totrans-163
  prefs: []
  type: TYPE_NORMAL
  zh: 总共有 5 人为这个版本做出了贡献。名字旁边带有“+”符号的人是第一次为该版本贡献补丁。
- en: Jeff Reback
  id: totrans-164
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: Jeff Reback
- en: Joris Van den Bossche
  id: totrans-165
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: Joris Van den Bossche
- en: MeeseeksMachine +
  id: totrans-166
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: MeeseeksMachine +
- en: Tom Augspurger
  id: totrans-167
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: Tom Augspurger
- en: jbrockmendel
  id: totrans-168
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: jbrockmendel
